<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Commodities in <%= state %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f7fafc;
            color: #2d3748;
        }

        header {
            background-color: #48bb78;
            padding: 1rem;
            text-align: center;
            color: #fff;
            font-size: 2rem;
            font-weight: bold;
            border-bottom: 4px solid #38a169;
        }

        main {
            max-width: 1200px;
            margin: 2rem auto;
            padding: 1rem;
        }

        h1 {
            text-align: center;
            margin-bottom: 2rem;
            font-size: 2.5rem;
            color: #2f855a;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 1.5rem;
        }

        .commodity-card {
            background-color: #fff;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            padding: 1rem;
            text-align: center;
            transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
            overflow: hidden;
            position: relative;
        }

        .commodity-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.2);
        }

        .commodity-card img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 12px 12px 0 0;
        }

        .commodity-content {
            padding: 1rem;
        }

        .commodity-content h2 {
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 1rem;
        }

        .commodity-content a {
            display: inline-block;
            margin-top: 1rem;
            text-decoration: none;
            background-color: #48bb78;
            color: #fff;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .commodity-content a:hover {
            background-color: #38a169;
        }

        a.back-link {
            display: inline-block;
            margin-top: 2rem;
            text-align: center;
            background-color: #48bb78;
            color: #fff;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        a.back-link:hover {
            background-color: #38a169;
        }

        footer {
            background-color: #48bb78;
            padding: 1rem;
            text-align: center;
            color: #fff;
            font-size: 1rem;
            margin-top: 2rem;
        }
    </style>
</head>

<body>
    <header>
        Commodities in <%= state %>
    </header>

    <main>
        <h1>Available Commodities in <%= state %></h1>
        <div class="grid">
            <% commodities.forEach(commodity => { %>
                <div class="commodity-card">
                    <img src="/images/<%= commodity %>.jpg" alt="Image of <%= commodity %>">
                    <div class="commodity-content">
                        <h2><%= commodity %></h2>
                        <a href="/state/<%= state %>/commodity/<%= commodity %>">View Markets</a>
                    </div>
                </div>
            <% }); %>
        </div>
        
        <a href="/" class="back-link">Back to States</a>
    </main>

    <footer>
        &copy; 2024 Agri Website. All Rights Reserved.
    </footer>
</body>

</html>
