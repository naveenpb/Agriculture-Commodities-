<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agri Website</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        main {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem 1rem;
        }

        .card {
            width: 300px;
            margin: 1rem;
            overflow: hidden;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.1);
            transition: transform 0.3s ease-in-out;
            position: relative;
        }

        .card:hover {
            transform: translateY(-10px);
        }

        .card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
            border-top-left-radius: 12px;
            border-top-right-radius: 12px;
        }

        .card:hover img {
            filter: brightness(80%);
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f7fafc;
        }

        .bg-gray-100 {
            background-color: #f7fafc;
        }

        .card-content {
            padding: 1rem;
        }

        .card-content h2 {
            font-size: 1.25rem;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .card-content p {
            color: #718096;
            margin-bottom: 0.5rem;
        }

        .view-details {
            display: inline-block;
            padding: 0.5rem 1rem;
            background-color: #48bb78;
            color: #fff;
            text-align: center;
            border-radius: 8px;
            text-decoration: none;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .view-details:hover {
            background-color: #38a169;
        }

        .flex {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
        }

        @media (max-width: 768px) {
            .card {
                width: 100%;
            }

            .card img {
                height: 150px;
            }
        }

    </style>
</head>

<body class="bg-gray-100">
    
    <header style="background-color: #48bb78; padding: 1rem; text-align: center; color: #fff; font-size: 1.5rem; font-weight: bold;">
        Agri Website
    </header>

    <main>
        <div class="flex">
            <% states.forEach(function(state) { %>
                <div class="card">
                    <img src="/images/<%= state.toLowerCase().replace(/ /g, '-') %>.jpg" alt="<%= state %>">
                    <div class="card-content">
                        <h2><%= state %></h2>
                        <p>Explore the agricultural markets of <%= state %></p>
                        <a href="/state/<%= state %>" class="view-details">View Details</a>
                    </div>
                </div>
            <% }); %>
        </div>
    </main>

    <footer style="background-color: #48bb78; padding: 1rem; text-align: center; color: #fff; font-size: 1rem;">
        &copy; 2024 Agri Website. All Rights Reserved.
    </footer>

</body>

</html>
